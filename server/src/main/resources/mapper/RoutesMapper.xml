<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cumtcdio.server.mapper.RoutesMapper">
    <resultMap id="BaseResultMap" type="cn.cumtcdio.server.model.Routes">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="path" jdbcType="VARCHAR" property="path"/>
        <result column="icon" jdbcType="VARCHAR" property="icon"/>
        <result column="parent_id" jdbcType="INTEGER" property="parentId"/>
    </resultMap>
    <select id="getRoutesByRoleId" resultMap="BaseResultMap">
        SELECT r.id,r.name,r.icon,r.path
        FROM routes AS r INNER JOIN routes_role AS rr ON r.id = rr.routes_id
        WHERE rr.role_id = #{rId} and r.parent_id = 0
    </select>
    <select id="getChildByParentId" resultMap="BaseResultMap">
        SELECT r.id,r.path,r.name,r.icon
        FROM routes AS r INNER JOIN routes_role AS rr ON r.id = rr.routes_id
        WHERE r.parent_id = #{id} and rr.role_id = #{rId}
    </select>
</mapper>