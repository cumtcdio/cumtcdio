<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.cumtcdio.server.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="cn.cumtcdio.server.model.Role">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="role_type" jdbcType="VARCHAR" property="roleType"/>
    </resultMap>
    <!--通过用户的编号username获取角色信息-->
    <select id="getRoleByUserId" resultMap="BaseResultMap">
        SELECT A.*
        FROM role A INNER JOIN user_role B ON A.id = B.role_id
        WHERE B.user_id = #{userId}
    </select>
    <!--获取所有角色的信息-->
    <select id="getAllRole" resultMap="BaseResultMap">
        SELECT *
        FROM role
    </select>
</mapper>